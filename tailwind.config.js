const colors = require("tailwindcss/colors");

module.exports = {
  theme: {
    extend: {
      colors: {
        orange: colors.orange,
        teal: colors.teal,
        lightblue: colors.lightBlue,
      },
    },
  },
};
